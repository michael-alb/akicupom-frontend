import { Http } from '@angular/http';
import { PromocoesService } from '../shared/promocoes.service';
/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PromocaoFormComponent } from './promocao-form.component';

describe('Component: PromocaoFormComponent', () => {
  it('should create an instance', () => {
    let component = new PromocaoFormComponent(this, this, this);
    expect(component).toBeTruthy();
  });
});
