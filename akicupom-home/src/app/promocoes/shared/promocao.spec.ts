/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import { Promocao } from './promocao';

describe('Promocao', () => {
  it('should create an instance', () => {
    expect(new Promocao()).toBeTruthy();
  });
});
